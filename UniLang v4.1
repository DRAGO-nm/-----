# ==============================
# 🌟 UniLang 4.1 — язык будущего
# Автор: Нур-Мухаммад
# ==============================

импорт система
импорт gui
импорт игровой_движок as engine
импорт мини_ИИ as ai
импорт плагины as plugins
импорт облако as cloud
импорт git_integration as git

печать("👋 Добро пожаловать в UniLang 4.1!")

# --- Подключение онлайн-плагинов ---
plugins.установить_онлайн("fizika")
plugins.установить_онлайн("npc_ai")
plugins.установить_онлайн("shaders")
plugins.установить_онлайн("ui_plus")
plugins.установить_онлайн("генератор_контента")  # новый ИИ для создания объектов
печать("Подключённые плагины:", plugins.список_плагинов())

# --- Создание облачного проекта ---
проект = cloud.НовыйПроект("Супер Игра 4.1", платформа="универсальная")
проект.сохранить_облако()

# --- Git интеграция ---
git.клонировать("https://github.com/username/unilang_project.git", в_папку="мой_проект")
git.загрузить(проект, "origin/main")

# --- Сцены ---
сцена_город = engine.Сцена("Город")
сцена_лес = engine.Сцена("Лес")

# --- Спрайты ---
герой = engine.Спрайт("герой.png", x=100, y=100, ширина=50, высота=50)
враг = engine.Спрайт("враг.png", x=500, y=300, ширина=50, высота=50)

# --- Физика ---
физика_движок = plugins.вызвать("fizika")
физика_движок.добавить(герой, масса=70, трение=0.3)
физика_движок.добавить(враг, масса=50, трение=0.2)

# --- AI для NPC ---
враг_ИИ = plugins.вызвать("npc_ai").НПС(враг, цель=герой, стратегия="следовать")
враг_ИИ.патруль([ (500,300), (700,300), (700,500), (500,500) ])  # патрулирование
engine.добавить_ИИ(враг_ИИ)

# --- Генерация контента через ИИ ---
генератор = plugins.вызвать("генератор_контента")
новый_дерево = генератор.создать("дерево", x=200, y=400, размер=50)
сцена_лес.добавить(новый_дерево)

# --- Добавляем спрайты в сцены ---
сцена_город.добавить(герой)
сцена_лес.добавить(враг)
сцена_лес.добавить(новый_дерево)

# --- Расширенный GUI ---
кнопка_уровень = gui.Кнопка("Повысить уровень", x=50, y=50, ширина=200, высота=50)
кнопка_уровень.действие = lambda: setattr(герой, "уровень", getattr(герой, "уровень",1)+1)

слайдер_скорость = gui.Слайдер("Скорость героя", x=50, y=120, ширина=200, min=1, max=10)
слайдер_скорость.при_изменении = lambda val: setattr(герой, "скорость", val)

окно = gui.Окно("UniLang 4.1 Game", 800, 600)
окно.добавить(кнопка_уровень)
окно.добавить(слайдер_скорость)

# --- Анимация и обновления ---
функция обновление():
    герой.x += getattr(герой, "скорость", 2)
    если герой.x > 800:
        герой.x = 0
    физика_движок.обновить(герой)
    физика_движок.обновить(враг)
    сцена_город.обновить()
    сцена_лес.обновить()

окно.каждый_кадр(lambda: обновление())

# --- Метапрограммирование ---
код = """
печать('🧠 UniLang 4.1: мульти-сцены + AI + генерация контента = супертехнология!')
"""
выполнить(код)

# --- Запуск игры ---
engine.запустить(окно)
печать("✅ UniLang 4.1 успешно завершила работу!")