# ==============================
# 🌟 UniLang — универсальный язык будущего
# Версия: 2.1
# Автор: Нур-Мухаммад
# ==============================

импорт система
импорт gui
импорт игровой_движок as engine
импорт мини_ИИ as ai
импорт плагины as plugins  # новая система плагинов

печать("👋 Добро пожаловать в UniLang 2.1!")

# --- Подключение плагинов ---
plugins.подключить_плагин("fizika")    # физика для игр
plugins.подключить_плагин("shaders")   # новые графические эффекты
plugins.подключить_плагин("npc_ai")    # расширенные ИИ для NPC

печать("Подключённые плагины:", plugins.список_плагинов())

# --- Игровой движок и GUI с плагинами ---
окно = gui.Окно("UniLang 2.1 Game", 800, 600)

герой = engine.Спрайт("герой.png", x=100, y=100, ширина=50, высота=50)
engine.добавить_спрайт(герой)

враг = engine.Спрайт("враг.png", x=500, y=300, ширина=50, высота=50)
engine.добавить_спрайт(враг)

# Используем ИИ из плагина
враг_ИИ = plugins.вызвать("npc_ai").НПС(враг, цель=герой, стратегия="патрулировать")
engine.добавить_ИИ(враг_ИИ)

# --- Пример GUI ---
кнопка_уровень = gui.Кнопка("Повысить уровень", x=50, y=50, ширина=200, высота=50)
кнопка_уровень.действие = lambda: setattr(герой, "уровень", getattr(герой, "уровень", 1)+1)
окно.добавить(кнопка_уровень)

# --- Анимация и кадры ---
функция анимация_героя():
    герой.x += 2
    если герой.x > 800:
        герой.x = 0

окно.каждый_кадр(lambda: анимация_героя())

# --- Метапрограммирование ---
код = "печать('🧠 UniLang 2.1 умеет подключать плагины и расширять функционал!')"
выполнить(код)

# --- Запуск проекта ---
engine.запустить(окно)
печать("✅ UniLang 2.1 завершила работу успешно!")