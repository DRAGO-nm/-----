# ==============================
# 🌟 UniLang — универсальный язык будущего
# Версия: 1.2
# Автор: Нур-Мухаммад
# ==============================

импорт случай
импорт система
импорт база_данных как bd
импорт asyncio
импорт поток
импорт gui  # встроенная мини-библиотека для GUI

# --- Переменные ---
строка пользователь = "Нур-Мухаммад"
число ширина_окна = 800
число высота_окна = 600

печать("👋 Привет,", пользователь)
печать("Добро пожаловать в UniLang 1.2!")

# --- Класс Игрок ---
класс Игрок:
    функция __init__(self, имя, уровень=1):
        self.имя = имя
        self.уровень = уровень

    функция повысить_уровень(self):
        self.уровень += 1
        печать(f"{self.имя} теперь уровень {self.уровень}!")

герой = Игрок(пользователь)

# --- GUI: окно и кнопки ---
окно = gui.Окно("UniLang Game", ширина_окна, высота_окна)

# Кнопка повышения уровня
кнопка_уровень = gui.Кнопка("Повысить уровень", x=50, y=50, ширина=200, высота=50)
кнопка_уровень.действие = λ: герой.повыcить_уровень()
окно.добавить(кнопка_уровень)

# Текст на экране
текст_инфо = gui.Текст(f"Привет, {герой.имя}! Уровень: {герой.уровень}", x=50, y=120)
окно.добавить(текст_инфо)

# --- Фигуры ---
круг = gui.Круг(x=400, y=300, радиус=50, цвет="синий")
окно.добавить(круг)

прямоугольник = gui.Прямоугольник(x=200, y=400, ширина=150, высота=80, цвет="красный")
окно.добавить(прямоугольник)

# --- События ---
функция обновление_текста():
    текст_инфо.текст = f"Привет, {герой.имя}! Уровень: {герой.уровень}"

окно.каждый_кадр(λ: обновление_текста())

# --- Простая анимация ---
функция анимация_круга():
    круг.x += 2
    если круг.x > ширина_окна:
        круг.x = 0

окно.каждый_кадр(λ: анимация_круга())

# --- Запуск игры ---
окно.запустить()
печать("✅ UniLang 1.2 завершила работу успешно!")