# ==============================
# 🌟 UniLang — универсальный язык будущего
# Версия: 1.1
# Автор: Нур-Мухаммад
# ==============================

импорт случай
импорт система
импорт база_данных как bd
импорт asyncio
импорт поток

# --- Переменные и типы ---
число попытки = 3
строка пользователь = "Нур-Мухаммад"
булев активен = True
конст МАКС_УРОВЕНЬ = 100

печать("👋 Привет,", пользователь)
печать("Добро пожаловать в UniLang 1.1!")

# --- Функции ---
функция сумма(a, b) -> число:
    вернуть a + b

функция максимум(список):
    вернуть max(список)

функция минимум(список):
    вернуть min(список)

функция сортировать_по(список, ключ=λ x: x):
    вернуть sorted(список, key=ключ)

# --- Классы ---
класс Игрок:
    функция __init__(self, имя, уровень=1):
        self.имя = имя
        self.уровень = уровень

    функция повысить_уровень(self):
        если self.уровень < МАКС_УРОВЕНЬ:
            self.уровень += 1
        печать(f"{self.имя} теперь {self.уровень}-го уровня!")

    функция __str__(self):
        вернуть f"Игрок {self.имя}, уровень {self.уровень}"

герой = Игрок(пользователь)
печать(герой)

# --- Коллекции ---
список чисел = [1,2,3,4,5,6,7,8,9]
список_квадратов = числа.карта(λ x: x*x)
чётные = числа.фильтр(λ x: x%2==0)
печать("Квадраты:", список_квадратов)
печать("Чётные:", чётные)
печать("Максимум:", максимум(числа))
печать("Минимум:", минимум(числа))
печать("Сортировка по убыванию:", сортировать_по(числа, λ x: -x))

# --- Асинхронность ---
асинх функция показать_задержку(сек):
    ждать сек
    печать(f"Прошло {сек} секунд.")

# запуск асинхронной задачи
asyncio.запустить(показать_задержку(2))

# --- Потоки ---
функция фоновая_печать(текст):
    для i в [1,2,3]:
        печать(f"{текст} ({i})")
        asyncio.ждать(1)

поток.запустить(фоновая_печать, "⌛ Фоновая задача выполняется...")

# --- Работа с файлами ---
с_файлом("лог.txt", "записать") как файл:
    файл.записать("UniLang 1.1 запущен успешно!\n")

с_файлом("лог.txt", "читать") как файл:
    печать("Содержимое файла:\n", файл.прочитать())

# --- Работа с базой данных ---
bd.создать_бд("игроки.db")
bd.добавить_запись("ученики", {"имя": "Нур-Мухаммад", "возраст": 18})
печать("Все ученики:", bd.выбрать_все("ученики"))
печать("Ученики старше 17:", bd.выбрать_по_условию("ученики", {"возраст": λ x: x>17}))

# --- Модули и случайности ---
печать("🎲 Случайное число:", случай.число(1, 100))
печать("🎰 Список случайных чисел:", [случай.число(1,10) для i в range(5)])

# --- Метапрограммирование ---
код = """
печать('🧠 Код сгенерировал сам себя!')
герой.представиться = λ: печать(f'Я — {герой.имя}, уровень {герой.уровень}')
герой.представиться()
"""
выполнить(код)

печать("✅ UniLang 1.1 завершила работу успешно!")